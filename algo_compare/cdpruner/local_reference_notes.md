# Local Reference Notes

This directory is reserved for the official CDPruner implementation. SCND and
other local diversity strategies must not be reported as official CDPruner.

The local NeXT compatibility wrapper changes model-family wiring and restores
canonical anyres preprocessing. It does not replace the official conditional-DPP
selection algorithm.
