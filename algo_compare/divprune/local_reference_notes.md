# Local Reference Notes

This method directory is for the official DivPrune implementation only.

Local exploratory methods from `entropy_exp` may be referenced in manifests as
`source_type: local_reference`, but they must not be labeled as official
DivPrune runs.
