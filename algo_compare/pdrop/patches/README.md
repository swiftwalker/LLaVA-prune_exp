# Patches

No official source patches are applied by default.

If a future compatibility patch is required, keep it as a small documented
patch file here and apply it explicitly from a wrapper script. Do not edit the
fetched `third_party/PyramidDrop` source in place without recording the change.
