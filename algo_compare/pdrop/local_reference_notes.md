# Local Reference Notes

`algo_compare/pdrop` is for official PyramidDrop runs only.

Current `entropy_exp` exploration strategies such as `sparsevlm_boost`,
`sparsevlm_entropy_alpha`, and hybrid SV2/Ours configurations may be referenced
as `source_type: local_reference` in manifests, but they must not be labeled as
official PDROP results.
