# SparseVLM Patches

Keep this directory for optional patch files against the official SparseVLM
checkout.

The default reproduction path should avoid patches. If a patch becomes
necessary, document:

- upstream commit
- patch purpose
- exact command used to apply it
- whether the result is still considered official or only adapted-official
