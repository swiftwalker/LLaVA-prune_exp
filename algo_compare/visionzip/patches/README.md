# VisionZip Patches

No source patches are required for the initial VisionZip wrapper.

The wrapper imports the official `visionzip` package from
`third_party/VisionZip` and applies it to the local LLaVA model at runtime.
