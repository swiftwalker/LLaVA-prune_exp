# FastV Local Reference Notes

`algo_compare/fastv` is for official FastV reproduction only. Local exploratory
methods from `entropy_exp` should be referenced as separate local-reference
manifest entries and must not be labeled as official FastV.
